import {NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const {admin}=await requireAdmin();const b=await req.json();const payload={code:String(b.code||'').trim().toUpperCase(),title:String(b.title||'').trim(),description:String(b.description||''),passing_progress:Number(b.passing_progress||80),is_published:b.is_published==='on'||b.is_published===true,updated_at:new Date().toISOString()};const {error}=await admin.from('courses').update(payload).eq('id',id);if(error)throw error;return NextResponse.json({ok:true});}catch(e:any){return NextResponse.json({error:e.message},{status:500});}}
